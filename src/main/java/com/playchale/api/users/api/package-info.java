/** What other modules may use from the users module. Everything in {@code internal} and {@code web} is its own. */
@NamedInterface("api")
package com.playchale.api.users.api;

import org.springframework.modulith.NamedInterface;
