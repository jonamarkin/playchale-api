/**
 * What other modules may use from the games module: the events it publishes when something happens
 * in a game. Notifications listens to them; payments will too.
 */
@NamedInterface("api")
package com.playchale.api.games.api;

import org.springframework.modulith.NamedInterface;
